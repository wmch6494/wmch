{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "youtube:test": "node --test scripts/youtube-parser.test.mjs",
    "youtube:check": "node scripts/sync-youtube.mjs",
    "youtube:check:gemini": "node scripts/sync-youtube.mjs --gemini",
    "youtube:sync": "node scripts/sync-youtube.mjs --write",
    "youtube:sync:gemini": "node scripts/sync-youtube.mjs --gemini --write"
  },
  "dependencies": {
    "astro": "^7.0.7"
  }
}
